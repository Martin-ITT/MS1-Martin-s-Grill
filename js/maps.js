/* Gogle maps */

function initMap(){
    var options = {
        zoom:14,
        center:{lat:53.3402575,lng:-6.2637580}
    }
    var map = new
    google.maps.Map(document.getElementById('map'), options);
}
    